// a poorly named class for holding the results of each iteration
class Pop {
  float x, y;
  color c;

  public Pop(float x, float y, color c) {
    this.x = x;
    this.y = y;
    this.c = c;
  }
}
